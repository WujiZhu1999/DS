1.思考让线性回归模型具有非线性能力

1）通过特征工程把一些特征转换成非线性的。比如说x变成f(x), f为非线性函数，然后做线性回归
2）通过分段线性

2.L1与L2回归分别具有什么特性
1）L1 是参数绝对值之和，所以参数趋向于变成0，这样相当于关掉了某些特征，所以当特征中有少数比较关键的特征用这个比较合适。
2）L2 是参数平方之和，所以当某个w已经很小的时候，再去见小这个w收益很小，所以不太会变成0，但是会比较平均的把所有w都压低，当模型有很多特征的时候，L2可以避免过度依赖某个特征。